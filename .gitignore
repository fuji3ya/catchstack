
# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Dev artifacts — HTML mockups + their reference images.
# Not part of the RN bundle; they're the source-of-truth for screen design,
# but they don't belong in the iOS repo / build output.
design/

# Local AsyncStorage / build / cache
.expo/
node_modules/
ios/build/
android/build/
scratchpad_*.txt
